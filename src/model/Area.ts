import { ModelElement } from "./ModelElement";
import { AreaSize, Position } from "./base-types";

/**
 * Represents a rectangle area inside the client area of a parent element.
 */
export interface Area extends ModelElement {
}